Procedure apu1
goto top
do while not eof()
  lc_code=substr(code,1,3)
  if code02 <>' ' and code02<'A'
    replace code02 with lc_code+code02
  endif
  lc_code=substr(code02,1,3)
  if code03 <>' ' and code03<'A'
    replace code03 with lc_code+code03
  endif
  lc_code=substr(code03,1,3)
  if code04 <>' ' and code04<'A'
    replace code04 with lc_code+code04
  endif
  lc_code=substr(code04,1,3)
  if code05 <>' ' and code05<'A'
    replace code05 with lc_code+code05
  endif
  lc_code=substr(code05,1,3)
  if code06 <>' ' and code06<'A'
    replace code06 with lc_code+code06
  endif
  lc_code=substr(code06,1,3)
  if code07 <>' ' and code07<'A'
    replace code07 with lc_code+code07
  endif
  lc_code=substr(code07,1,3)
  if code08 <>' ' and code08<'A'
    replace code08 with lc_code+code08
  endif
  lc_code=substr(code08,1,3)
  if code09 <>' ' and code09<'A'
    replace code09 with lc_code+code09
  endif
  lc_code=substr(code09,1,3)
  if code10 <>' ' and code10<'A'
    replace code10 with lc_code+code10
  endif
  lc_code=substr(code10,1,3)
  if code11 <>' ' and code11<'A'
    replace code11 with lc_code+code11
  endif
  lc_code=substr(code11,1,3)
  if code12 <>' ' and code12<'A'
    replace code12 with lc_code+code12
  endif
  lc_code=substr(code12,1,3)
  if code13 <>' ' and code13 <'A'
    replace code13 with lc_code+code13
  endif
  lc_code=substr(code13,1,3)
  if code14 <>' ' and code14<'A'
    replace code14 with lc_code+code14
  endif
  lc_code=substr(code14, 1,3)
  if code15 <>' ' and code15<'A'
    replace code15 with lc_code+code15
  endif
  lc_code=substr(code15, 1,3)
  if code16 <>' ' and code16<'A'
    replace code16 with lc_code+code16
  endif
  lc_code=substr(code16, 1,3)
  if code17 <>' ' and code17<'A'
    replace code17 with lc_code+code17
  endif
  lc_code=substr(code17, 1,3)
  if code18 <>' ' and code18<'A'
    replace code18 with lc_code+code18
  endif
  lc_code=substr(code18, 1,3)
  if code19 <>' ' and code19<'A'
    replace code19 with lc_code+code19
  endif
  lc_code=substr(code19, 1,3)
  if code20 <>' ' and code20<'A'
    replace code20 with lc_code+code20
  endif
  lc_code=substr(code20, 1,3)
  if code21 <>' ' and code21<'A'
    replace code21 with lc_code+code21
  endif
  lc_code=substr(code21, 1,3)
  if code22 <>' ' and code22<'A'
    replace code22 with lc_code+code22
  endif
  lc_code=substr(code22, 1,3)
  if code23 <>' ' and code23<'A'
    replace code23 with lc_code+code23
  endif
  lc_code=substr(code23, 1,3)
  if code24 <>' ' and code24<'A'
    replace code24 with lc_code+code24
  endif
  lc_code=substr(code24, 1,3)
  if code25 <>' ' and code25<'A'
    replace code25 with lc_code+code25
  endif
   lc_code=substr(code25, 1,3)
  if code26 <>' ' and code26<'A'
    replace code26 with lc_code+code26
  endif
  lc_code=substr(code26, 1,3)
  if code27 <>' ' and code27<'A'
    replace code27 with lc_code+code27
  endif
  lc_code=substr(code27, 1,3)
  if code28 <>' ' and code28<'A'
    replace code28 with lc_code+code28
  endif
  lc_code=substr(code28, 1,3)
  if code29 <>' ' and code29<'A'
    replace code29 with lc_code+code29
  endif
  lc_code=substr(code29, 1,3)
  if code30 <>' ' and code30<'A'
    replace code30 with lc_code+code30
  endif
  lc_code=substr(code30, 1,3)
  if code31 <>' ' and code31<'A'
    replace code31 with lc_code+code31
  endif
  lc_code=substr(code31, 1,3)
  if code32 <>' ' and code32<'A'
    replace code32 with lc_code+code32
  endif
   lc_code=substr(code32, 1,3)
  if code33 <>' ' and code33<'A'
    replace code33 with lc_code+code33
  endif
  lc_code=substr(code33, 1,3)
  if code34 <>' ' and code34<'A'
    replace code34 with lc_code+code34
  endif
  lc_code=substr(code34, 1,3)
  if code35 <>' ' and code35<'A'
    replace code35 with lc_code+code35
  endif
  lc_code=substr(code35, 1,3)
  if code36 <>' ' and code36<'A'
    replace code36 with lc_code+code36
  endif
  lc_code=substr(code36, 1,3)
  if code37 <>' ' and code37<'A'
    replace code37 with lc_code+code37
  endif
  lc_code=substr(code37, 1,3)
  if code38 <>' ' and code38<'A'
    replace code38 with lc_code+code38
  endif
  lc_code=substr(code38, 1,3)
  if code39 <>' ' and code39<'A'
    replace code39 with lc_code+code39
  endif
  lc_code=substr(code39, 1,3)
  if code40 <>' ' and code40<'A'
    replace code40 with lc_code+code40
  endif
  lc_code=substr(code40, 1,3)
  if code41 <>' ' and code41<'A'
    replace code41 with lc_code+code41
  endif
  lc_code=substr(code41, 1,3)
  if code42 <>' ' and code42<'A'
    replace code42 with lc_code+code42
  endif
  lc_code=substr(code42, 1,3)
  if code43 <>' ' and code43<'A'
    replace code43 with lc_code+code43
  endif
  lc_code=substr(code43, 1,3)
  if code44 <>' ' and code44<'A'
    replace code44 with lc_code+code44
  endif
  lc_code=substr(code44, 1,3)
  if code45 <>' ' and code45<'A'
    replace code45 with lc_code+code45
  endif
   lc_code=substr(code45, 1,3)
  if code46 <>' ' and code46<'A'
    replace code46 with lc_code+code46
  endif
  lc_code=substr(code46, 1,3)
  if code47 <>' ' and code47<'A'
    replace code47 with lc_code+code47
  endif
  lc_code=substr(code47, 1,3)
  if code48 <>' ' and code48<'A'
    replace code48 with lc_code+code48
  endif
  lc_code=substr(code48, 1,3)
  if code49 <>' ' and code49<'A'
    replace code49 with lc_code+code49
  endif
  lc_code=substr(code49, 1,3)
  if code50 <>' ' and code50<'A'
    replace code50 with lc_code+code50
  endif
  lc_code=substr(code50, 1,3)
  if code51 <>' ' and code51<'A'
    replace code51 with lc_code+code51
  endif
  lc_code=substr(code51, 1,3)
  if code52 <>' ' and code52<'A'
    replace code52 with lc_code+code52
  endif
  lc_code=substr(code51, 1,3)
  if code52 <>' ' and code52<'A'
    replace code52 with lc_code+code52
  endif
  lc_code=substr(code52, 1,3)
  if code53 <>' ' and code53<'A'
    replace code53 with lc_code+code53
  endif
  lc_code=substr(code53, 1,3)
  if code54 <>' ' and code54<'A'
    replace code54 with lc_code+code54
  endif
  lc_code=substr(code54, 1,3)
  if code55 <>' ' and code55<'A'
    replace code55 with lc_code+code55
  endif
  lc_code=substr(code55, 1,3)
  if code56<>' ' and code56<'A'
    replace code56 with lc_code+code56
  endif
  lc_code=substr(code56, 1,3)
  if code57 <>' ' and code57<'A'
    replace code57 with lc_code+code57
  endif
  lc_code=substr(code57, 1,3)
  if code58 <>' ' and code58<'A'
    replace code58 with lc_code+code58
  endif
  lc_code=substr(code58, 1,3)
  if code59 <>' ' and code59<'A'
    replace code59 with lc_code+code59
  endif
  skip
enddo
return